# Preferred public canonical origin (no path or trailing query).
# Vercel falls back to VERCEL_PROJECT_PRODUCTION_URL when this is empty.
# Local builds fall back to http://localhost:4321 and are marked noindex.
SITE_URL=
